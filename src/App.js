import React, { Component } from 'react';
import { ButtonApp } from './docs';

export default class App extends Component {
  render() {
    return (
      <div>
        <ButtonApp />
      </div>
    );
  }
}
